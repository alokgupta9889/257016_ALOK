#list: are used to store the multiple values in the single variable

    a=["alok,shivam,"'manas',12]
    print(type(a))

    # a=(1,"ranchit","abc","que")
    # print(type(a))

    # list : are used to store the multiple values in the single variable but tuples are immutable

    # a=(1,"ranchit","abc","que")
    # print(a[:3])

    #sets: are used to store the multiple values in the single variable but sets are unorderd and unidexed

    # a=(1,2,2,2,"ranchit","shivam")
    #print(a)
    #Dictionary: are used to store the multiple value in the single variable in key:value pair 

    a={"Name":"ranchit","Age":21,"college":"ABC"}
    print(a"[name]")






     